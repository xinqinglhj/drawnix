version: '3'
services:
  drawnix:
    build:
      context: .
      dockerfile: Dockerfile.deploy
    image: drawnix-server
    ports:
      - "3000:3000"
    restart: always
    environment:
      - PORT=3000
    volumes:
      - ./data:/app/data  # Optional: Persist SQLite DB
