### 问题1

#### 描述

需要解决如下问题： 

1. 打开本地列表 查看和 编辑报错： drawing-service.ts:27   GET http://localhost:3000/api/drawings/1 404 (Not Found)

2. 云端列表，点击 下载到本地 提示： Sync failed: "undefined" is not valid JSON

#### 解决

已修复您提到的两个问题：

1. **本地列表报错**：修复了“云端文件对话框”未正确传递本地标识的问题，现在打开本地文件应能正确调用本地存储服务，不再请求服务器接口。
2. **同步报错**：修复了同步时因列表数据不完整（缺少data字段）导致的 JSON 解析错误。现在系统会在同步前自动补全获取画作详情。